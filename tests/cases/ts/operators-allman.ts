const a = b?.c?.[0] ?? d;
if (x!==y && z===w) {}
let p = 2**3; p **= 2; q ??= 1; r ||= 2; s &&= 3;
function f(a?: string, ...rest: number[]): Map<string, number> {
return x! + y;
}
const t = c ? 1 : 2;
interface I { a?: number; b: string }
const f2 = (x) => x * 2;
const obj = { a: 1, ...o };
function* gen() { yield* other(); }
const m = a*b;
const [s2, setS] = useState<Array<A | B>>([]);
type R<T extends object = {}> = Record<"a" | "b", (x: T) => void>;
const mm = new Map<string, { a: number; b: string }>();
if (a < b && c > d) {}
for (let i = 0; i < n; i++) {}
const x = cond ? {
a: 1
} : null;
export interface QueueState extends Record<string, { state: FetchStatus; options: BackendSrvRequest }> {}
