use std::io::{Write, Read, self};
use std::fmt;

struct Point {
    pub x: i32,
    label_text: String
}

fn f(
    a: i32,
    b: i32,
) {
    let total: i64 = 0;
    let mut name = String::new();
}
